# Sprint 16: Theme & Dark Mode

**Prerequisite:** Read every document in `/information` before starting.

## Goal
Finalize the visual design, ensuring complete dark mode support and a polished landing page.

## Tasks
1. Implement the `ThemeToggle` component in the header.
2. Ensure all pages use semantic CSS variables (`bg-paper`, `text-ink`) from `index.css`.
3. Redesign the Landing Page with cinematic hero images and modern layouts.
4. Integrate the official `logo-etimad.svg` (light/dark aware).

## Acceptance Criteria
- [ ] Dark mode toggles instantly and persists in `localStorage`.
- [ ] No hardcoded colors (`#FAFAFA`, `bg-white`) break the dark mode experience.
- [ ] The landing page matches the requested modern, professional Palestinian aesthetic.
