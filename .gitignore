# dependencies
node_modules/

# environment & secrets (C-4, NFR-S4) — never commit a filled-in env file
.env
.env.*
!.env.example
server.env

# locally stored uploads (L-4: local disk storage in the MVP)
uploads/

# build output
dist/

# logs
*.log
npm-debug.log*
